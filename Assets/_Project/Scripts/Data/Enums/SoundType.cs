public enum SoundType
    {
        Chop,        // Tiếng chặt gỗ
        Lose,        // Tiếng thua
        ButtonClick, // Tiếng bấm các Button trên UI
        BGM          // Nhạc nền (Background Music)
    }